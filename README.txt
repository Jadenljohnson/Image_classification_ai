An Electron desktop that classifies images of cats and dogs.The app runs entirely on your machine and uses a locally trained ResNet-18 model.
Steps to run
1.git clone
2. npm install
3. pip install torch torchvision pillow
4. npm start